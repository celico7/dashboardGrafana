{% extends 'base.html.twig' %}

{% block title %}Espace Gestionnaire{% endblock %}

{% block body %}
    <style>
        .dashboard-shell { min-height: 100vh; background: #0f172a; color: #e2e8f0; padding: 48px 32px; font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif; }
        .dashboard-card { max-width: 1100px; margin: 0 auto; background: #0b1220; border: 1px solid rgba(255, 255, 255, 0.08); border-radius: 20px; padding: 32px; box-shadow: 0 20px 60px rgba(0,0,0,0.35); }
        .dash-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 24px; gap: 16px; }
        .dash-title { font-size: 28px; font-weight: 700; letter-spacing: -0.01em; }
        .dash-sub { color: #94a3b8; margin-top: 8px; }
        .pill { display: inline-flex; align-items: center; gap: 8px; background: #1e293b; color: #cbd5e1; padding: 10px 14px; border-radius: 999px; font-size: 14px; }
        .pill span { font-weight: 600; color: #f97316; }
        .grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(240px, 1fr)); gap: 16px; margin-top: 24px; }
        .tile { background: #0f172a; border: 1px solid rgba(255,255,255,0.06); border-radius: 16px; padding: 18px; }
        .tile h3 { margin: 0 0 10px 0; font-size: 18px; }
        .tile p { margin: 0; color: #cbd5e1; line-height: 1.5; }
        .link-row { margin-top: 28px; display: flex; justify-content: flex-end; gap: 12px; flex-wrap: wrap; }
        .link-row a { color: #c4d056; font-weight: 600; text-decoration: none; }
        .link-row a:hover { text-decoration: underline; }
    </style>

    <div class="dashboard-shell">
        <div class="dashboard-card">
            <div class="dash-header">
                <div>
                    <div class="dash-title">Espace Gestionnaire</div>
                    <div class="dash-sub">Connecté en tant que {{ app.user.fullname ?? 'gestionnaire' }} ({{ app.user.email ?? '' }})</div>
                    <div class="pill">Rôle actif : <span>GESTIONNAIRE</span></div>
                </div>
            </div>

            <div class="grid">
                <div class="tile">
                    <h3>Vue globale</h3>
                    <p>Supervisez l’ensemble des bâtiments, alertes et indicateurs clés.</p>
                </div>
                <div class="tile">
                    <h3>Administration</h3>
                    <p>Gérez les accès, rôles et paramètres de l’application.</p>
                </div>
                <div class="tile">
                    <h3>Rapports</h3>
                    <p>Générez des rapports consolidés et partagez-les avec les équipes.</p>
                </div>
                <div class="tile">
                    <h3>Accès Grafana</h3>
                    <p>Consultez les dashboards Grafana étendus pour la supervision.</p>
                </div>
            </div>

            <div class="link-row">
                <a href="#">Ouvrir Grafana</a>
                <a href="{{ path('app_logout') }}">Se déconnecter</a>
            </div>
        </div>
    </div>
{% endblock %}
