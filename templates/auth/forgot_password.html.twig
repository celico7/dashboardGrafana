<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mot de passe oublié - Tableau de Bord Énergétique</title>
    <link rel="stylesheet" href="{{ asset('css/auth.css') }}">
</head>
<body>
    <div class="auth-container">
        <div class="auth-left">
            <div class="auth-card">

                <div class="auth-header">
                    <h1>Réinitialiser votre mot de passe</h1>
                    <p>Entrez l'adresse email de votre compte</p>
                </div>

                <form method="POST" class="auth-form">
                    <div class="form-group">
                        <label>Email</label>
                        <div class="input-wrapper">
                            <input type="email" name="email" placeholder="votre.email@iut.fr" required value="{{ email }}">
                            <svg class="input-icon" viewBox="0 0 24 24"><path d="M20 4H4c-1.1 0-2 .9-2 2v12c0 1.1.9 2 2 2h16c1.1 0 2-.9 2-2V6c0-1.1-.9-2-2-2zm0 4l-8 5-8-5V6l8 5 8-5v2z"/></svg>
                        </div>
                        {% if errors.email is defined %}
                            <div class="error-message">
                                {% for error in errors.email %}
                                    {{ error }}<br>
                                {% endfor %}
                            </div>
                        {% endif %}
                    </div>

                    <button type="submit" class="btn-login">Confirmer</button>
                </form>

                <div class="auth-footer">
                    <p><a href="{{ path('app_login') }}">← Retour à la connexion</a></p>
                </div>
            </div>

            <div class="auth-credits">
                <p>Développé par 
                    <a href="https://github.com/melindaland" target="_blank" class="credit-name">ALAND Mélinda</a>, 
                    <a href="https://baslan67.github.io/portfolio/" target="_blank" class="credit-name">ASLAN Baran</a> et 
                    <a href="https://github.com/celico7" target="_blank" class="credit-name">HOFFMANN Célia</a> - 2026
                </p>
            </div>
        </div>

        <div class="auth-right">
            <img src="{{ asset('images/iut-haguenau.png') }}" alt="Bâtiment de l'IUT de Haguenau">
        </div>
    </div>
</body>
</html>
